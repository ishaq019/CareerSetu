# CareerSetu backend — SLIM runtime dependencies for Vercel serverless (<250 MB).
# This file is what Vercel installs. Local-only tooling (uvicorn, alembic,
# pytest) lives in requirements-dev.txt and is intentionally excluded here.
#
# Core web + persistence stack (required for the API, auth and guest analysis).
fastapi>=0.115,<1.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
SQLAlchemy>=2.0.30
PyJWT>=2.8
httpx>=0.27
email-validator>=2.1

# Document parsing
pypdf>=4.2
python-docx>=1.1

# PostgreSQL / Neon driver. `binary` ships prebuilt libpq; the `pool` extra is
# dropped because serverless functions use short-lived NullPool connections.
psycopg[binary]>=3.1

# --- AI stack (optional at runtime; returns 503 if the LLM key is unset) -------
# Only needed for LLM/RAG features (analysis enhancement, chat, interview,
# resume/cover-letter generation).
langchain>=0.3
langchain-text-splitters>=0.3
langchain-groq>=0.2
langgraph>=0.2

# Chroma Cloud access via the THIN HTTP client. This is a subset of the full
# `chromadb` package and pulls NONE of onnxruntime / tokenizers / numpy /
# grpcio / kubernetes / opentelemetry — the packages that pushed the bundle
# well past Vercel's 250 MB limit. Retrieval here is lexical (no embeddings),
# so the server/embedding machinery in full `chromadb` is unnecessary.
chromadb-client>=0.5
